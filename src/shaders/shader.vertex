$input  a_position
$output v_texCoord

#include <bgfx_shader.sh>

void main() {
    v_texCoord = a_position.xy * 0.5 + 0.5; // Convert [-1,1] to [0,1] range
    gl_Position = vec4(a_position, 0.0, 1.0); // Set position
}
